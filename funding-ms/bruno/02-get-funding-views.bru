meta {
  name: Get Funding Views
  type: http
  seq: 2
}

request {
  method: GET
  url: {{base_url}}/api/v1/funding/requests/views
  params {
    funderId: {{funder_id}}
  }
  headers {
    Accept: application/json
  }
}

assertions {
  expect status == 200
}

